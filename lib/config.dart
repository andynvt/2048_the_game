import 'package:flutter/material.dart';

class Config {
  Config._();
  static final radius = BorderRadius.circular(8);
  static final circleRadius = BorderRadius.circular(100);
  static String version = 'v1.0.0';
  static String about =
      'Created by AndyNVT Studio. Based on 1024 by Veewo Studio and conceptually similar to Threes by Asher Vollmer.';
  static String howToPlay =
      'Swipe up/down/left/right to move the tiles. When two tiles with the same number touch, they merge into one!';
}
